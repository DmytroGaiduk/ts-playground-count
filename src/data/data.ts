export type CounterProps = {
    value: number;
    maximumValue: number;
    initialValue: number
}

const counterData: CounterProps = {
    initialValue: 12,
    value: 0,
    maximumValue: 42
}

export default counterData